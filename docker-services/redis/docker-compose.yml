services:
  keydb:
    image: eqalpha/keydb:latest
    container_name: keydb_dev
    ports:
      - "6379:6379"
    volumes:
      - keydb_data:/data
    command: keydb-server /etc/keydb/keydb.conf --server-threads 2 --protected-mode no
    restart: always

volumes:
  keydb_data: